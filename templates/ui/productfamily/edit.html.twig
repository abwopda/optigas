{% extends 'layout.html.twig' %}

{% block body %}
    <h3><span class="glyphicon glyphicon-send"></span> Modification d'une famille</h3>
    <form enctype="multipart/form-data" action="{{ path('productfamily.update', { 'id': entity.id }) }}"  class="form-horizontal col-md-8" method="post">
        {% include "ui/productfamily/form.html.twig" %}

        <div class="form-group">
            <div class="col-sm-offset-6 col-sm-6">
                <button type="submit" class="btn btn-primary"><span class="glyphicon glyphicon-edit"></span> Mettre à jour</button>
                <a href="{{ path('productfamily.show', { 'id': entity.id }) }}" class="btn btn-default"><span class="glyphicon glyphicon-search"></span> Afficher</a>
                {#<a href="{{ path('pos.delete', { 'id': entity.id }) }}" class="btn btn-danger"><span class="glyphicon glyphicon-search"></span> Supprimer</a> #}
            </div>
        </div>
    </form>
{% endblock %}