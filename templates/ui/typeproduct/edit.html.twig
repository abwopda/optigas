{% extends 'layout.html.twig' %}

{% block body %}
    <h3><span class="glyphicon glyphicon-send"></span> Modification d'un type de produit</h3>
    <form enctype="multipart/form-data" action="{{ path('typeproduct.update', { 'id': entity.id }) }}"  class="form-horizontal col-md-8" method="post">
        {% include "ui/typeproduct/form.html.twig" %}

        <div class="form-group">
            <div class="col-sm-offset-6 col-sm-6">
                <button type="submit" class="btn btn-primary"><span class="glyphicon glyphicon-edit"></span> Mettre à jour</button>
                <a href="{{ path('typeproduct.show', { 'id': entity.id }) }}" class="btn btn-default"><span class="glyphicon glyphicon-search"></span> Afficher</a>
                {#<a href="{{ path('typeproduct.delete', { 'id': entity.id }) }}" class="btn btn-danger"><span class="glyphicon glyphicon-search"></span> Supprimer</a> #}
            </div>
        </div>
    </form>
{% endblock %}