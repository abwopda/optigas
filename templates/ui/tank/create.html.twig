{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_rest(form) }}
    <button class="btn btn-primary">Créer</button>
    {{ form_errors(form) }}
    {{ form_end(form) }}
{% endblock %}