{% extends 'base.html.twig' %}

{% block body %}
<h3>Création d'une nouvelle pompe</h3>
    <form action="{{ path('pump.create', {'tank': entity.tank.id}) }}" method="post">
        {% include "ui/pump/form.html.twig" %}
        <button type="submit" class="btn btn-primary">Créer</button>
    </form>
{% endblock %}